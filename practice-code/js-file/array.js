// array is  Array is a way to store multiple values in one variable.
// or array explain 
//An array is like a container or box that can hold multiple items in one place.


// write in a simple example in Array 
let studentdetails =["nishan shrestha ", " rima ",12 ,10 ];// array in multiple value in used
//console.log(`this is  student details-\n ${studentdetails}`) ;
console.log(`this is student details \n${JSON.stringify(studentdetails)}`);// to array from in output 
//or 
let  rolls =[1,3,2,4,2,4,2,5,2];// in array in number
//JSON.stringify() converts data into a JSON-formatted string.to used in  back tip 
 
console.log(` this is array number is-\n ${JSON.stringify(rolls)}`);// it mustly used for in js 
//console.log(` this is array number is-\n[ ${rolls}]`);// to used in simple in but 

//Accessing Array Values 
console.log(` to print in array  index value${studentdetails[1]} `);
//Changing Array Value 
console.log(` change in value in array ${studentdetails[2]=3434}`);// to change in array index 2 in  value change or update

console.log(studentdetails);// to print in array new update 

//Array Length
console.log(studentdetails.length);// to in array length or value in count 
// Add item in array 
studentdetails.push("shrestha");  // end in add in array to used in push 

console.log(studentdetails); 





























//push() add element in end point 
//unshift() add in element in first 
//pop() removed in element in end point 
//shift (remove in element in first
 
